// Selecciona el botón por su ID
const button = document.getElementById("saludarBtn");
        
// Usa addEventListener para agregar el evento click
button.addEventListener("click", function () {
    alert("Hola desde el DOM!");
});